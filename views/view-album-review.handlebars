<div class="d-flex">
    <div class="card p-2">
        <div class="card-header group-header">
            <h1>Album Review</h1>
            <h5>{{sendAlbumRevData.album.name}}</h5>
            <p>by {{sendAlbumRevData.album.artist.name}}</p>
        </div>
        <div class="d-flex card-body group-body">
            <div class="card p-2">
                <section class="card-header">
                    <h5>{{sendAlbumRevData.title}}</h5>
                    <i>by {{sendAlbumRevData.user.name}}</i>
                </section>
                <section class="card-body">
                    <i>{{sendAlbumRevData.review}}</i>
                </section>
            </div>
        </div>
    </div>
</div>
        

<script src="/js/deleteReview.js"></script>