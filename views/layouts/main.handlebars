<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Site</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</head>

<body>
    <header>
        <div class="border-box p-3 text-center">
        <h1 >Album Review Zone</h1>
        <i>You Can Also Review Songs</i>
        </div>
         <nav class="d-flex justify-content-evenly align-items-center text-center">
            <a href="/">
                <button type="button" class="btn btn-success shadow-lx" id="home">Home</button>
            </a>
             
            {{#if loggedIn}}
            <a href="/dash">
                <button type="button" class="btn btn-primary shadow-lx" id="home">Dash</button>
            </a>
            <button type="button" class="btn btn-danger shadow-lx" id="logout">Log Out</button>
            {{else}}
            <a href="/login">
                <button type="button" class="btn btn-info shadow-lx" id="home">Log In</button>
            </a>
            {{/if}}
        </nav>
    </header>

    <main>
        {{{body}}}
    </main>

    <footer>
    {{#if logged_in}}

    {{/if}}

    </footer>
</body>
        <script src="/js/logout.js"></script>
</html>